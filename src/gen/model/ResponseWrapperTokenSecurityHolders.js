/*
 * GoPlus Security API Document
 * No description provided (generated by Swagger Codegen https://github.com/swagger-api/swagger-codegen)
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.45
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';
import {ResponseWrapperTokenSecurityLockedDetail} from './ResponseWrapperTokenSecurityLockedDetail';

/**
 * The ResponseWrapperTokenSecurityHolders model module.
 * @module model/ResponseWrapperTokenSecurityHolders
 * @version 1.0
 */
export class ResponseWrapperTokenSecurityHolders {
  /**
   * Constructs a new <code>ResponseWrapperTokenSecurityHolders</code>.
   * @alias module:model/ResponseWrapperTokenSecurityHolders
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>ResponseWrapperTokenSecurityHolders</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/ResponseWrapperTokenSecurityHolders} obj Optional instance to populate.
   * @return {module:model/ResponseWrapperTokenSecurityHolders} The populated <code>ResponseWrapperTokenSecurityHolders</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new ResponseWrapperTokenSecurityHolders();
      if (data.hasOwnProperty('is_locked'))
        obj.is_locked = ApiClient.convertToType(data['is_locked'], 'Number');
      if (data.hasOwnProperty('is_contract'))
        obj.is_contract = ApiClient.convertToType(data['is_contract'], 'Number');
      if (data.hasOwnProperty('address'))
        obj.address = ApiClient.convertToType(data['address'], 'String');
      if (data.hasOwnProperty('balance'))
        obj.balance = ApiClient.convertToType(data['balance'], 'String');
      if (data.hasOwnProperty('locked_detail'))
        obj.locked_detail = ApiClient.convertToType(data['locked_detail'], [ResponseWrapperTokenSecurityLockedDetail]);
      if (data.hasOwnProperty('tag'))
        obj.tag = ApiClient.convertToType(data['tag'], 'String');
      if (data.hasOwnProperty('percent'))
        obj.percent = ApiClient.convertToType(data['percent'], 'String');
    }
    return obj;
  }
}

/**
 * It describes whether the tokens owned by the holder are locked \"1\" means true; \"0\" means false;  (3) \"tag\" describes the address's public tag. Example:Burn (Notice:About \"locked\": We only support the token lock addresses or black hole addresses that we have included. )
 * @member {Number} is_locked
 */
ResponseWrapperTokenSecurityHolders.prototype.is_locked = undefined;

/**
 * It describes whether the holder is a contract \"1\" means true; \"0\" means false.
 * @member {Number} is_contract
 */
ResponseWrapperTokenSecurityHolders.prototype.is_contract = undefined;

/**
 * It describes the holder address; 
 * @member {String} address
 */
ResponseWrapperTokenSecurityHolders.prototype.address = undefined;

/**
 * It describes the balance of the holder. 
 * @member {String} balance
 */
ResponseWrapperTokenSecurityHolders.prototype.balance = undefined;

/**
 * It is an array, decribes lock position info of this holder, only shows when \"locked\": 1. This Array may contain multiple objects for multiple locking info. (Notice:When \"locked\":0, or lock address is a black hole address,  \"locked_detail\" will be no return.)
 * @member {Array.<module:model/ResponseWrapperTokenSecurityLockedDetail>} locked_detail
 */
ResponseWrapperTokenSecurityHolders.prototype.locked_detail = undefined;

/**
 * It describes the address's public tag. Example:Burn Address/Deployer; 
 * @member {String} tag
 */
ResponseWrapperTokenSecurityHolders.prototype.tag = undefined;

/**
 * It  describes the percentage of tokens held by this holder (Notice:About \"percent\": 1 means 100% here.)
 * @member {String} percent
 */
ResponseWrapperTokenSecurityHolders.prototype.percent = undefined;

